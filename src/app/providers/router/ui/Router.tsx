import { Route, Routes } from 'react-router-dom';

const Router = () => {
  return (
    <div>
      <Routes>
        <Route path="/" element={< />} />
      </Routes>
    </div>
  );
};

export default Router;
